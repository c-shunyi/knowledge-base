### 后端响应巨量数据，前端如何处理

- 请求网路性能问题
	- 后端调优，改接口规格，减少数据量
	- 流式读取 fetch stream，先接受响应头后就进行处理，再等待响应体进行后续处理，解析json可以写用渐进式的json解析器
		  const res = await fetch(xxxxxx) // 等待请求头
		  const body = await res.json() // 等待请求体
- 页面构建性能问题
	- 数据过多可以分页渲染
	- 虚拟列表
	- canvas

命名规范